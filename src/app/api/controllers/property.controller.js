import { HomeInterest, Property } from "../models/propertyModel.js"
import User from "../models/userModel.js"

export const createProperty = async (req, res) => {
    const {propertyId, homeInterestId} = req.body
}